# Core
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-multipart>=0.0.9
python-dotenv>=1.0.0

# PostgreSQL driver + connection pool (only used when DB_BACKEND=postgres — see db/pg.py)
psycopg[binary]>=3.1
psycopg-pool>=3.2

# Cloudflare R2 (S3-compatible) object storage (only used when STORAGE_BACKEND=r2 — see storage/r2.py)
boto3>=1.34

# OpenAI — LLM audit layer + translation
openai>=1.30.0

# Audio processing
pydub>=0.25.1
ffmpeg-python>=0.2.0

# Speech-to-text (local Whisper fallback)
openai-whisper>=20231117

# Sarvam AI — Indian language STT
requests>=2.31.0
httpx>=0.27.0

# Retry logic for the STT cascade (transcription/stt.py) — was installed
# locally outside of requirements.txt tracking, missing here, which is
# what broke every real audit on Render ("No module named 'tenacity'")
tenacity>=8.2.0

# Speaker diarization (optional — needs HF_TOKEN and GPU recommended)
# pyannote.audio>=3.1.0
# torch>=2.0.0

# Sentiment / NLP (optional ML layer — falls back to rule-based if unavailable)
# transformers>=4.40.0
# torch>=2.0.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
openpyxl
gdown>=5.0

# Report generation
reportlab>=4.0.0

# Deck (.pptx) ingestion (deck/ingestion/pptx_extractor.py) — same gap as
# tenacity above, installed locally but missing from this file
python-pptx>=1.0.0

# Google Sheets export (OAuth + Sheets/Drive API)
google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.130.0
